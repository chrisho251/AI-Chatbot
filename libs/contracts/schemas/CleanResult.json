{
  "additionalProperties": false,
  "description": "dropped is true when the text must not reach the model, such as after an injection hit.",
  "properties": {
    "dropped": {
      "default": false,
      "title": "Dropped",
      "type": "boolean"
    },
    "injection_hits": {
      "default": 0,
      "minimum": 0,
      "title": "Injection Hits",
      "type": "integer"
    },
    "pii_hits": {
      "default": 0,
      "minimum": 0,
      "title": "Pii Hits",
      "type": "integer"
    },
    "producer": {
      "default": "unknown",
      "title": "Producer",
      "type": "string"
    },
    "request_id": {
      "title": "Request Id",
      "type": "string"
    },
    "schema_version": {
      "default": "1.1",
      "title": "Schema Version",
      "type": "string"
    },
    "text": {
      "title": "Text",
      "type": "string"
    }
  },
  "required": [
    "request_id",
    "text"
  ],
  "title": "CleanResult",
  "type": "object"
}
